datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  email    String? @unique
  id       BigInt  @id @default(autoincrement())
  name     String
  password String?
  role     Json
}
